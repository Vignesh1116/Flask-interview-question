31. What is Flask-Login?

Flask-Login manages user authentication and sessions.

32. What is user authentication?

Authentication verifies user identity during login.

33. What is session in Flask?

A session stores user data between requests.

34. What is cookie in Flask?

A cookie stores small data on the user's browser.

35. Difference between session and cookie

Session → stored on server
Cookie → stored on client browser

Flask API
36. What is a REST API?

A REST API allows applications to communicate over HTTP.

37. What is Flask-RESTful?

Flask-RESTful is an extension for building REST APIs easily.

38. What is JSON in Flask?

Flask commonly returns data in JSON format.

39. What is jsonify()?

jsonify() converts Python objects to JSON response.

40. What is API authentication?

API authentication verifies client identity before accessing API.